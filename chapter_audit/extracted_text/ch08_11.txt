Chapter 8, Problem 11. Show that if f l(E) changes sign as E varies, then the ap-
propriate modification of dσ/dΩ in the weight-function method is to write dσ = |f l(E)|2 dΩ.
Obtain an estimate on the norm of the Lippmann–Schwinger integral operator using the
weight function.
Solution.
In the partial-wave expansion of the scattering amplitude, each partial wave contributes:
f l(E) = 1
k
X
l
(2l + 1)Tl(E)Pl(cos θ),
where Tl is the T -matrix element for angular momentum l.
When the Born approximation is used, f l(E) can change sign as E varies (unlike the
exact partial-wave amplitude which satisfies unitarity). In the weight-function method, the
scattering cross section involves:
dσ
dΩ = |f (θ)|2.
If f l(E) changes sign, we cannot simply use f l as a weight function since it becomes
negative. Instead, we use |f l(E)|2 directly:
dσ = |f l(E)|2 dΩ.
For the weight-function estimate of the norm, we use w(r) = |V (r)| and the weighted
Hilbert–Schmidt norm:
∥K∥2
w =
Z ∞
0
Z ∞
0

sin(kr<)
k
eikr>
1

2
|V (r)|2 |V (r′)| dr dr′.
The key estimate is that the free Green’s function satisfies |Gl
0(r, r′)| ≤ r</k for s-waves,
giving:
∥K∥2
w ≤ 1
k2
Z ∞
0
r2|V (r)||V (r)|dr
Z ∞
0
|V (r′)|dr′.
When f l changes sign, the Born approximation passes through zero at some energy,
indicating a Ramsauer–Townsend minimum. The weight-function method still provides a
valid upper bound on the norm, ensuring convergence of the Born series when ∥K∥w < 1,
i.e., at suﬀiciently high energies. The modification dσ = |f l|2 dΩ simply ensures that the
cross section remains positive definite regardless of the sign of the amplitude. ■
1